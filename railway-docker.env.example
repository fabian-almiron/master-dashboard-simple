# =============================================
# RAILWAY DOCKER DEPLOYMENT ENVIRONMENT VARIABLES
# =============================================
# Set these in your Railway project environment variables
# DO NOT commit actual values to version control

# =============================================
# AUTHENTICATION & SECURITY (Clerk)
# =============================================
# CRITICAL: Use LIVE keys for production (pk_live_* and sk_live_*)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your_live_publishable_key_here
CLERK_SECRET_KEY=sk_live_your_live_secret_key_here
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/master

# Admin emails (comma-separated) - UPDATE WITH REAL EMAILS
ADMIN_EMAILS=admin@yourdomain.com,owner@yourdomain.com

# CRITICAL: NEVER set BYPASS_AUTH to true in production
BYPASS_AUTH=false

# =============================================
# DATABASE CONFIGURATION
# =============================================

# Master Dashboard Database
NEXT_PUBLIC_MASTER_SUPABASE_URL=https://your-master-project.supabase.co
NEXT_PUBLIC_MASTER_SUPABASE_ANON_KEY=your-master-anon-key
MASTER_SUPABASE_SERVICE_ROLE_KEY=your-master-service-key

# NOTE: Shared CMS Database removed - only using Master Dashboard Database

# =============================================
# API CONFIGURATION
# =============================================

# Vercel API (for deployments)
VERCEL_TOKEN=your-vercel-api-token
VERCEL_TEAM_ID=your-vercel-team-id

# Bitbucket API (for repositories)
BITBUCKET_USERNAME=your-bitbucket-username
BITBUCKET_API_TOKEN=your-bitbucket-api-token
BITBUCKET_WORKSPACE=your-workspace
BITBUCKET_MASTER_REPO=cms-master

# AI Services
ANTHROPIC_API_KEY=your-anthropic-api-key

# =============================================
# RAILWAY SPECIFIC CONFIGURATION
# =============================================
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1
RAILWAY_ENVIRONMENT=production

# Application Settings
NEXT_PUBLIC_MASTER_DASHBOARD_URL=https://your-railway-app.railway.app

# Default Settings
DEFAULT_CMS_REPO=https://bitbucket.org/yourworkspace/cms-master.git
DEFAULT_CMS_BRANCH=main
DEFAULT_VERCEL_REGION=iad1

# =============================================
# RAILWAY DEPLOYMENT NOTES
# =============================================
# 1. Set these variables in Railway dashboard under "Variables" tab
# 2. Railway will automatically use PORT environment variable
# 3. Update NEXTAUTH_URL to your Railway app URL
# 4. Update CORS origins in vercel.json to include Railway domain
# 5. Make sure all OAuth redirect URIs include Railway domain
